DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
PORT=8080
JWT_SECRET=rahasia-untuk-development-saja
FRONTEND_URL=http://localhost:3000
GIN_MODE=debug


# Seeder options
# SEED_PASSWORD=changeme        # Override default password for all seeded accounts (default: password123)
# FORCE_SEED=true               # Force seed even in production (GIN_MODE=release)
# SKIP_SEED=true                # Skip seeding entirely
# SEED_TEST_STUDENTS=100        # Create N load-test student accounts
